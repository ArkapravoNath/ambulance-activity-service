version: '2'
services:
  ambulanceactivityservice-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/ambulanceactivityservice/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=ambulanceactivityservice
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
